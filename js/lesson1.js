let r = prompt ("Enter value of r") ; /// get value from user
const a = Math.PI
let R = ( ( 2 * a ) * r ) ;
let S = a * (r * r ) ;
alert(" Perimeter of circle is " + R +  "Area of circle is " + S + "." ) ;
